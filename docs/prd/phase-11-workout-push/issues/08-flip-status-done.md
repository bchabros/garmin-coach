# 08 - Flip Phase 11 to Done + record spike outcome

Status: ready-for-agent
Blocked by: 06, 07
Sources: `docs/prd/phase-11-workout-push/PRD.md`, ADR 0013.

## Goal

Close the phase in the docs the way every prior phase closed: flip the status,
link the ADR, and record where strength/HIIT landed.

## Scope

- Flip Phase 11 to **Done** in `README.md` and `docs/PROJECT.md`, linking the PRD and
  ADR 0013 (mirror the existing phase rows).
- Record the strength spike outcome (works -> follow-up issue reference; fallback ->
  the documented local-only decision).
- Follows the repo's `DOCS:` commit convention.

## Acceptance criteria

- [ ] README + PROJECT status rows show Phase 11 Done with PRD + ADR links.
- [ ] The strength spike outcome is recorded in the roadmap.
- [ ] `task check` green.
